services:
  - type: web
    name: saturn-relay
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python saturn_relay.py
    envVars:
      - key: RELAY_SECRET
        generateValue: true
      - key: PORT
        value: 10000
