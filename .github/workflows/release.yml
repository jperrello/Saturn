name: Release Saturn Router

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag (e.g., v1.0.0)'
        required: true
        type: string

env:
  CARGO_TERM_COLOR: always

jobs:
  build-mipsel:
    name: Build MIPSEL (soft-float)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build cross-compilation image
        run: |
          cd saturn-router
          docker build -t saturn-mips-builder -f cross/Dockerfile.mipsel .

      - name: Build Saturn binary
        run: |
          cd saturn-router
          mkdir -p target
          docker run --rm \
            -v "${{ github.workspace }}/saturn-router:/project" \
            -w /project \
            saturn-mips-builder \
            bash -c "
              cargo +nightly build --release --target mipsel-unknown-linux-musl \
                --no-default-features --features rustls \
                -Zbuild-std=std,panic_abort && \
              cp target/mipsel-unknown-linux-musl/release/saturn target/saturn-mipsel-sf && \
              chmod 644 target/saturn-mipsel-sf
            "
          ls -la target/saturn-mipsel-sf

      - name: Get binary info
        run: |
          cd saturn-router/target
          file saturn-mipsel-sf
          ls -lh saturn-mipsel-sf

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: saturn-mipsel-sf
          path: saturn-router/target/saturn-mipsel-sf

  release:
    name: Create Release
    needs: [build-mipsel]
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download MIPSEL artifacts
        uses: actions/download-artifact@v4
        with:
          name: saturn-mipsel-sf
          path: artifacts/

      - name: Get version
        id: version
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "version=${{ inputs.tag }}" >> $GITHUB_OUTPUT
          else
            echo "version=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT
          fi

      - name: Create checksums file
        run: |
          cd artifacts
          sha256sum saturn-* > checksums.txt
          cat checksums.txt

      - name: Create release body
        run: |
          cat << 'EOF' > release_body.md
          ## Saturn Router ${{ steps.version.outputs.version }}

          Pre-built binary for OpenWRT routers.

          ### Quick Install

          ```bash
          curl -sSL https://raw.githubusercontent.com/jperrello/Saturn/main/install-router.sh | sh
          ```

          ### Manual Install

          ```bash
          wget https://github.com/jperrello/Saturn/releases/download/${{ steps.version.outputs.version }}/saturn-mipsel-sf
          scp saturn-mipsel-sf root@192.168.8.1:/tmp/saturn
          chmod +x /tmp/saturn
          ```

          ### Supported Architectures

          - **mipsel-sf**: GL.iNet GL-MT300N-V2 (Mango) and similar soft-float MIPS routers

          ### Checksums

          See `checksums.txt` for SHA256 hashes.
          EOF

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.version }}
          name: Saturn Router ${{ steps.version.outputs.version }}
          body_path: release_body.md
          files: |
            artifacts/saturn-mipsel-sf
            artifacts/checksums.txt
          draft: false
          prerelease: false
